version: '3.8'
services:
  react-ui:
    build:
      context: ./
    ports:
      - "3000:3000"
    container_name: sendami-tech-blog
    stdin_open: true
    tty: true
    volumes:
      - ./:/sendami-tech-blog
      - /node-modules    